---
description: Example slash command - replace with your description
allowed-tools: Read, Grep, Glob
---

# Example Command

This command demonstrates the slash command format.

## Arguments Received

$ARGUMENTS

## Instructions

1. Replace this content with your command's instructions
2. Use `$ARGUMENTS` to access passed arguments
3. Use `$1`, `$2` etc. for positional arguments
4. Use `!`command`` to embed bash output dynamically

## Dynamic Context Example

Current directory: !`pwd`
